import Color from './colorbase';

class XMenuBackColor extends Color {
  /**
   * 构造函数
   *
   * @param {Object} editor 编辑器的对象
   */
  constructor(editor) {
    super(editor, 'backcolor');
  }
}
/**
 * XMenuBackColor 模块.
 * @module XMenuBackColor
 */
export default XMenuBackColor;
