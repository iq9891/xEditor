import Base from './base';
/**
* XMenuBold 对象
* @example
* new XMenuBold(editor);
*/
class XMenuBold extends Base {
  /**
   * 构造函数
   *
   * @param {Object} editor 编辑器的对象
   */
  constructor(editor) {
    super(editor, 'bold');
  }
}
/**
 * XMenuBold 模块.
 * @module XMenuBold
 */
export default XMenuBold;
