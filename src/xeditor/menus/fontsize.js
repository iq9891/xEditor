import FontBase from './fontbase';
/**
* XMenuFontsize 对象
* @example
* new XMenuFontsize(editor);
*/
class XMenuFontsize extends FontBase {
  /**
   * 构造函数
   *
   * @param {Object} editor 编辑器的对象
   */
  constructor(editor) {
    super(editor, 'fontsize');
  }
}
/**
 * XMenuFontsize 模块.
 * @module XMenuFontsize
 */
export default XMenuFontsize;
