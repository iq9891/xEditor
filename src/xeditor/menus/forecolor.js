import Color from './colorbase';

class XMenuForeColor extends Color {
  /**
   * 构造函数
   *
   * @param {Object} editor 编辑器的对象
   */
  constructor(editor) {
    super(editor, 'forecolor');
  }
}
/**
 * XMenuForeColor 模块.
 * @module XMenuForeColor
 */
export default XMenuForeColor;
