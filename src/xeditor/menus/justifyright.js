import Base from './base';
/**
* XMenuJustifyright 对象
* @example
* new XMenuJustifyright(editor);
*/
class XMenuJustifyright extends Base {
  /**
   * 构造函数
   *
   * @param {Object} editor 编辑器的对象
   */
  constructor(editor) {
    super(editor, 'justifyright');
  }
}
/**
 * XMenuJustifyright 模块.
 * @module XMenuJustifyright
 */
export default XMenuJustifyright;
